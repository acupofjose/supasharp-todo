@page "/upcoming"
@using SupasharpTodo.Shared.Interfaces
@using SupasharpTodo.BlazorWASM.Shared.Auth
@using SupasharpTodo.BlazorWASM.Shared.Todos
@inject IAppStateService AppState;

<PageTitle>Supa#Todos</PageTitle>

@if (AppState.IsLoggedIn)
{
    <TodosView SelectedView="TodosView.TodoViewType.Upcoming"/>
}
else
{
    <LoginView/>
}

@code {

    protected override void OnInitialized()
    {
        AppState.PropertyChanged += (_, _) => StateHasChanged();
    }

}

